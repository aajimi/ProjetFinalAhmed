{% extends 'baseF.html.twig' %}

{% block content %}
    <h1>Modifier une réclmation</h1>

    {{ form_start(edit_reclamationForm) }}

    <div class="form-group">{{ form_row(edit_reclamationForm.sujet,{ 'attr' : {'class' : 'form-control'}}) }}</div>

    <div class="form-group col-md-6">{{ form_row(edit_reclamationForm.dateReclamation) }}</div>
    <div class="form-group">{{ form_row(edit_reclamationForm.flagTraite,{'attr' : {'class' : 'form-control'}}) }}</div>
    <div class="form-group">
        <input type="submit" class="btn btn-warning"value="Envoyez Reclamation" />
    </div>
    {{ form_end(edit_reclamationForm) }}

    <ul>
        <li>
            <a href="{{ path('ShowReclamationList') }}">Back to the list of reclamation</a>
        </li>
    </ul>
{% endblock %}